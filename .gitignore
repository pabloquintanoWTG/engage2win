# secrets
.env
.env.local
.env.*.local

# map photos — never commit
/samples/*
!/samples/README.md

# harness output
/validate/output/
/validate/review.html

# app — uploaded map photos (never commit)
/app/uploads/*
!/app/uploads/.gitkeep

# app — local SQLite database (never commit)
/app/*.db

# python
__pycache__/
*.pyc
.venv/
venv/

# node (if app is built later)
node_modules/
.next/
out/
dist/

# os / editor
.DS_Store
.vscode/
.idea/
